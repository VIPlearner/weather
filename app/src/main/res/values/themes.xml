<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.Weather" parent="android:Theme.Material.Light.NoActionBar" />

    <!-- Splash Screen Theme (Compatible with all Android versions) -->
    <style name="Theme.Weather.SplashScreen" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@drawable/splash_screen</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>

</resources>